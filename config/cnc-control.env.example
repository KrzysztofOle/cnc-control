# Sciezka do obrazu USB Mass Storage
CNC_USB_IMG=/var/lib/cnc-control/cnc_usb.img

# Rozmiar nowego obrazu USB tworzonego automatycznie (MB)
CNC_USB_IMG_SIZE_MB=1024

# Punkt montowania obrazu (upload G-code)
CNC_MOUNT_POINT=/mnt/cnc_usb

# Katalog do uploadu plikow z WebUI
CNC_UPLOAD_DIR=/mnt/cnc_usb

# Sciezka do repozytorium cnc-control (wykorzystywana przez uslugi systemd)
CNC_CONTROL_REPO=/home/cnc/cnc-control

# Globalna blokada trybu AP (true/false)
# false = AP zablokowany w WebUI i backendzie
CNC_AP_ENABLED=false
